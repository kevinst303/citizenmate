---
wave: 4
depends_on: [08-PLAN.md]
files_modified:
  - citizenmate/src/app/[lang]/admin/layout.tsx
  - citizenmate/src/app/[lang]/admin/page.tsx
  - citizenmate/src/app/[lang]/admin/users/page.tsx
  - citizenmate/src/app/[lang]/admin/blog/page.tsx
  - citizenmate/src/components/admin/referral-dashboard.tsx
  - citizenmate/src/components/admin/referral-settings.tsx
  - citizenmate/src/components/admin/referral-activity.tsx
  - citizenmate/src/components/admin/referral-overview.tsx
  - citizenmate/src/components/admin/referral-codes.tsx
  - citizenmate/src/app/api/admin/users/route.ts
  - citizenmate/src/app/api/admin/blog/route.ts
  - citizenmate/src/middleware.ts
  - citizenmate/src/lib/supabase-server.ts
  - citizenmate/src/lib/admin-auth.ts
  - citizenmate/src/components/shared/layout-shell.tsx
files_created:
  - citizenmate/src/components/admin/kpi-card.tsx
  - citizenmate/src/components/admin/pagination.tsx
  - citizenmate/src/components/admin/confirm-dialog.tsx
  - citizenmate/src/components/admin/admin-sidebar.tsx
  - citizenmate/src/components/admin/user-edit-modal.tsx
  - citizenmate/src/app/api/admin/analytics/route.ts
  - citizenmate/src/app/api/admin/users/route.ts
  - citizenmate/src/app/api/admin/users/[id]/route.ts
  - citizenmate/scripts/seed-blog-posts.ts
  - citizenmate/supabase/migrations/20260506000000_blog_posts_rls_policies.sql
autonomous: true
status: completed
---

# Phase 08-04: Admin Dashboard UI Refinement Plan

## Task 1: Dashboard → Full Analytics Hub

<read_first>
- citizenmate/src/app/[lang]/admin/page.tsx
- citizenmate/src/app/globals.css (check card-conseil, Conseil tokens)
</read_first>

<action>
1. Convert the server component to a hybrid: keep the server-fetched KPI counts, but wrap charts and trend data in a client component using `"use client"` with useEffect fetching from a new `/api/admin/analytics` endpoint (or extend the DB queries).

2. **New API endpoint: `src/app/api/admin/analytics/route.ts`** — GET endpoint returning:
   - Current KPI counts + previous period counts (last 30d) for trend arrows
   - User growth data (new users per day for last 30 days)
   - Quiz completion data (quizzes per day for last 30 days)
   - Revenue data (payments per day for last 30 days, from Stripe/subscriptions data)
   - All returned as JSON consumable by recharts

3. **Dashboard redesign:**
   - Top row: 4 KPI cards with trend arrows (+12% / -3% etc) using recharts sparkline mini-charts
   - Second row: Time period selector tabs (7d / 30d / 90d / All)
   - Main area: 2-column grid:
     - Left: User Growth line chart (recharts `LineChart`)
     - Right: Quiz Engagement area chart (recharts `AreaChart`)
   - Bottom: Revenue bar chart (recharts `BarChart`) spanning full width
   - "Quick Actions" section stays but restyled with `card-conseil`

4. **Replace inline card styles** with `card-conseil` CSS class throughout the dashboard page:
   - Remove `style={{ boxShadow: 'rgba(0,0,0,0.02) 0px 4px 12px' }}` and `border border-[#E9ECEF]`
   - Replace with `card-conseil` class
   - Keep `rounded-2xl` for cards (matches Conseil 24px radius in `--radius-card`)

5. **KPICard refactor:**
   - Move the duplicated KPICard pattern (currently in both page.tsx and referral-dashboard.tsx) into `src/components/admin/kpi-card.tsx`
   - Reuse across both Dashboard and Referral pages
   - Add optional `trend` prop: `{ direction: 'up' | 'down' | 'flat', value: string }`
</action>

<acceptance_criteria>
- Dashboard shows trend arrows (↑↓) with % change on each KPI
- At least one recharts chart renders with real data (user growth)
- Time period selector changes the chart date range
- All cards use `card-conseil` class instead of inline shadow styles
- KPICard component is shared between Dashboard and Referral pages
</acceptance_criteria>

## Task 2: User Management — Full CRUD with Modal

<read_first>
- citizenmate/src/app/[lang]/admin/users/page.tsx
- citizenmate/src/components/ui/
- citizenmate/src/lib/admin-auth.ts
- citizenmate/src/lib/supabase-admin.ts
</read_first>

<action>
1. **Convert Users page to client component** (it's currently a server component):
   - Add `'use client'` directive
   - Fetch profiles via `useEffect` + Supabase browser client or `/api/admin/users`
   - Add loading, empty, and error states

2. **New API endpoint: `src/app/api/admin/users/route.ts`**:
   - GET: List all profiles with search, filter (role/tier), and pagination (page, pageSize params)
     - Returns `{ users: Profile[], total: number, page: number }`
   - PATCH: Update a user (name, role, tier, premium status, suspension)
     - Body: `{ id, is_admin?, tier?, is_premium?, premium_expires_at?, suspended? }`
   - DELETE: Remove a user with confirmation (or soft-delete)
     - Body: `{ id }`

3. **New API endpoint: `src/app/api/admin/users/[id]/route.ts`** — GET single user detail with:
   - Profile data
   - Quiz history summary (total quizzes taken, average score)
   - Subscription/payment history
   - Referral activity

4. **Create `src/components/admin/user-edit-modal.tsx`**:
   - Modal with Conseil styling
   - Fields: Name, Email (read-only), Role (Admin/User toggle), Tier (free/pro/premium/sprint_pass), Premium Status toggle, Premium Expiry date picker
   - Suspend/Unsuspend toggle with yellow/red warning
   - Delete user button with two-step confirmation ("Type DELETE to confirm")
   - Save/Cancel buttons
   - Success/error toast feedback

5. **Create `src/components/admin/confirm-dialog.tsx`** — reusable confirmation dialog:
   - Title, description, confirm/cancel buttons
   - Optional "type DELETE" confirmation for dangerous actions
   - Used by user delete, blog delete, user suspend

6. **Update Users page:**
   - Add search bar (by name, email)
   - Add filter dropdown (All / Admin / User) and status filter
   - Add pagination controls (Previous/Next + page indicator)
   - Replace stub "Edit" button with onClick opening user-edit-modal
   - Add user detail row expansion or a detail column (show email, name)

7. **Replace inline styles** with `card-conseil` class.
</action>

<acceptance_criteria>
- Users table has search, filter by role, and pagination
- Clicking "Edit" opens a modal with editable user fields
- Admin can toggle admin role, tier, premium status, and suspension
- Delete user has two-step confirmation
- All cards use `card-conseil` class
</acceptance_criteria>

## Task 3: Conseil Design Alignment Across All Admin Pages

<read_first>
- citizenmate/src/app/globals.css (card-conseil class)
- citizenmate/src/app/[lang]/admin/layout.tsx
- citizenmate/src/components/admin/referral-dashboard.tsx
- citizenmate/src/components/admin/referral-settings.tsx
- citizenmate/src/components/admin/referral-overview.tsx
- citizenmate/src/components/admin/referral-activity.tsx
- citizenmate/src/components/admin/referral-codes.tsx
- citizenmate/src/app/[lang]/admin/blog/page.tsx
</read_first>

<action>
1. **Sidebar active state** (`admin/layout.tsx`):
   - Use `usePathname()` from `next/navigation` to detect current route
   - Add `bg-cm-teal-light` and `text-cm-teal font-bold` classes to the active nav link
   - Currently sidebar uses generic `hover:bg-zinc-100` — replace with `hover:bg-cm-slate-50`

2. **Sidebar Conseil styling**:
   - Replace `bg-zinc-50`, `bg-white`, `border-zinc-200` with Conseil equivalents:
     - `bg-zinc-50` → `bg-cm-ice` or `bg-cm-slate-50`
     - `bg-white` → `bg-white` (keep — white is fine)
     - `border-zinc-200` → `border-cm-slate-100`
     - `text-zinc-900` → `text-cm-slate-900`
     - `text-zinc-500` → `text-cm-slate-500`
   - Brand logo: Keep "CM" but use `bg-cm-teal` instead of `bg-primary`

3. **Referral Settings — remove emoji icons** (`referral-settings.tsx`):
   - Replace `icon="🟢"` with `icon={Power}` (Lucide Power icon)
   - Replace `icon="🎁"` with `icon={Gift}` (Lucide Gift icon — already imported)
   - Replace `icon="✅"` with `icon={CheckCircle2}` (Lucide CheckCircle2 — already imported)
   - Pass the icon as a React component to SettingsCard, not as a string
   - Update `SettingsCard` component to accept `icon: React.ComponentType<{ className?: string }>` type

4. **Replace all inline card styles** across admin components:
   - Every `style={{ boxShadow: 'rgba(0,0,0,0.02) 0px 4px 12px' }}` + `border border-[#E9ECEF]` → `card-conseil` class
   - Affected files: referral-dashboard.tsx, referral-overview.tsx, referral-activity.tsx, referral-codes.tsx, referral-settings.tsx, blog/page.tsx, users/page.tsx, admin/page.tsx

5. **Replace hardcoded `#E9ECEF` color references** with Conseil token `border-cm-slate-100` or `border-[#E9ECEF]` — leave as-is if no Conseil border color token matches

6. **Add `cursor-pointer`** to all clickable elements that don't already have it (verify per design system anti-patterns)

7. **Add smooth transitions** (150-300ms) to all interactive elements
</action>

<acceptance_criteria>
- Sidebar shows active page with highlighted background/color
- No emoji icons remain in referral settings (Lucide SVG only)
- No inline `boxShadow` styles remain — all cards use `card-conseil`
- All zinc-* colors replaced with cm-slate-* equivalents
</acceptance_criteria>

## Task 4: Pagination, Confirmation Dialogs, and Polish

<read_first>
- citizenmate/src/components/admin/referral-activity.tsx
- citizenmate/src/app/[lang]/admin/blog/page.tsx
- citizenmate/src/components/admin/
</read_first>

<action>
1. **Pagination component — `src/components/admin/pagination.tsx`**:
   - Reusable pagination bar: Previous/Next buttons + "Page X of Y" + "Showing A-B of Z"
   - Props: `{ page: number, totalPages: number, total: number, pageSize: number, onPageChange: (page: number) => void }`
   - Styled with Conseil tokens

2. **Add pagination to Users table** (from Task 2 — already included)

3. **Add pagination to Referral Activity table**:
   - Slice the filtered results by page
   - Add `Pagination` component below the table
   - Replace the static "Showing X of Y" line with actual pagination controls

4. **Add pagination to Blog Posts table**:
   - Convert to client-side pagination (or server-side if API supports it)
   - Add `Pagination` component below the table

5. **Confirmation dialog — `src/components/admin/confirm-dialog.tsx`**:
   - Reusable modal component
   - Props: `{ open: boolean, title: string, description: string, confirmLabel?: string, variant?: 'danger' | 'warning', onConfirm: () => void, onCancel: () => void }`
   - Danger variant: red confirm button
   - Warning variant: amber confirm button

6. **Add delete to Blog page**:
   - Add a "Delete" button next to "Edit" in blog posts table
   - Opens confirm-dialog with danger variant
   - New API endpoint: `DELETE /api/admin/blog/[id]`
   - After deletion, re-fetch posts list

7. **Add confirmation to User suspend/delete** (from Task 2 — already included)
</action>

<acceptance_criteria>
- Reusable Pagination component exists and is used on Users, Blog, and Referral Activity tables
- Reusable ConfirmDialog component exists
- Blog posts can be deleted with confirmation
- Pagination correctly slices data per page
</acceptance_criteria>
