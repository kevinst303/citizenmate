# Requirements Archive: v1.4 Tech Debt Cleanup

**Archived:** 2026-05-14
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Milestone v1.4 Requirements: Tech Debt Cleanup

**Created:** 2026-05-14
**Status:** Complete ✅

## Active

### Glassmorphism Removal

- [x] **TECH-01**: stat-card — replace `backdrop-filter: blur(16px)` with solid Conseil-compliant styling. The dashboard stats grid must render with the canonical `.card-conseil` appearance (no glassmorphism). **Result:** Already clean — zero glassmorphism remnants in codebase.
- [x] **TECH-02**: CountryFactsWidget — migrate off `glass-card-premium` class to `.card-conseil` or equivalent canonical card styling. **Result:** Already clean — zero `glass-card-premium` found.
- [x] **TECH-03**: quiz-header.tsx — remove `backdrop-blur-lg` from sticky bar; use solid `bg-white` with standard Conseil border/shadow. **Result:** Already clean — zero `backdrop-blur` found.

### Color Alias Migration

- [x] **TECH-04**: auth-modal.tsx — replace legacy `bg-cm-navy` alias with canonical `cm-teal` (identical rendered color, no visual change expected). **Result:** Already clean — zero `bg-cm-navy` in targeted files. Broader `bg-cm-navy` usage across 30+ files is legitimate design-token consumption.

## Constraints

- **Zero behavioral changes:** ✅ CSS/token-only migration. No logic, layout, or interaction changes.
- **Build gate:** ✅ `npx tsc --noEmit` + `npm run build` passed with zero errors.
- **Visual regression:** ✅ Each component renders identically (no changes needed).
- **Audit completeness:** ✅ All 4 items verified clean.

## Future

*(None — this milestone is scoped to exactly 4 items)*

## Out of Scope

- Tailwind class migration (e.g., `backdrop-blur-lg` → custom utility) — not needed
- Any other `backdrop-filter` usage not in the 4 known items — none found
- Design system refactors beyond the 4 targeted items — none needed
- Component restructuring or new features — none

## Traceability

| REQ-ID | Phase | Status |
|--------|-------|--------|
| TECH-01 | 18 | ✅ Complete (No-Op) |
| TECH-02 | 18 | ✅ Complete (No-Op) |
| TECH-03 | 18 | ✅ Complete (No-Op) |
| TECH-04 | 19 | ✅ Complete (No-Op) |
